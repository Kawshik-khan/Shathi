# Python 3.14 Compatible Requirements
# Uses Pydantic v1 (no Rust core) + SQLite (no C++ needed)

fastapi==0.104.1
uvicorn[standard]==0.34.0
pydantic==1.10.13
sqlalchemy[asyncio]==2.0.44
aiosqlite==0.20.0
alembic==1.14.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.9
httpx==0.28.0
openai>=1.35.0
structlog==24.4.0

# Optional: Pinecone (pure Python)
pinecone==5.4.0

# Optional: Supabase (pure Python)
supabase==2.13.0

# Note: transformers and torch excluded (require compilation)
# Note: asyncpg excluded (requires C++ build tools)
# For AI features, use OpenAI API only

